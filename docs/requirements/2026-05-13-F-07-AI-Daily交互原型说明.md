# AI Daily v1.0 交互原型说明

> **更新：2026-05-14** | 原型已部署线上，1007 行单文件 HTML + Chart.js

## 1. 页面结构

### 1.1 页面总览

```
桌面端 5 区域布局

┌──────────────────────────────────────┐
│  BP Overlay（冷启动引导）              │  ← 全屏遮罩
│  ♥ 今日 AI 脉搏                       │
│  [点击测量今日 AI 血压]                │
│  120    AI 圈血压 / 100               │
│  [进入智脉 →]                         │
├──────────────────────────────────────┤
│  Zone 1: Hero                        │  ← 问候 + 幸运模型 + 建议
│  "下午好"                             │     + 冷笑话（可切换）
│  ★ 幸运模型：Claude 4.7              │
│  宜：深度思考  忌：不看文档就调 API    │
├──────────────────────────────────────┤
│  Zone 2: AI 今日脉搏                  │  ← 3 条分类摘要
│  🖥 新模型：title1；title2            │
│  🏢 行业动态：title1；title2；title3  │
│  ⚙ 工具产品：title1；title2；title3  │
├──────────────────────────────────────┤
│  Zone 3: 趋势快照 (3 cards)           │
│  [最智能模型] [最实惠模型] [最快模型]    │
├──────────────────────────────────────┤
│  Zone 4: 大模型趋势看板                │  ← Tab 切换
│  [总榜] [文本] [图片] [视频]           │     模态选择
│  [智力] [价格] [速度]                  │     维度选择
│  ┌─ 图表 ─┐ ┌─ 雷达图 ─┐             │
│  │ 柱状图  │ │ 多维对比 │             │
│  └────────┘ └─────────┘              │
│  ┌─ 数据表 ──────────────────────┐   │
│  │ 模型 | 智力 | 编码 | 推理     │   │
│  └──────────────────────────────┘   │
├──────────────────────────────────────┤
│  Zone 5: 资讯列表                     │
│  [全部] [新模型] [大厂战略] [工具推荐] │  ← 筛选栏
│  展开/收起 → 毒舌 → 摘要 → 原文链接    │
└──────────────────────────────────────┘
```
│  │ ▸ 展开阅读 ▼          │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 🏷 工具推荐            │  │
│  │ Cursor 发布 2.0，      │  │
│  │ 支持多 Agent 协作       │  │
│  │ 即刻 · 8小时前         │  │
│  │ ▸ 展开阅读 ▼          │  │
│  └───────────────────────┘  │
│          ...                 │  10-15 条
├─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┤  ← 分割线
│  📊 大模型趋势看板            │
│  [性能]  [价格]  [速度]      │  ← 趋势 Tab 切换
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │ 综合评分 Top 5         │  │  ← 柱状图区
│  │ ██████████ o3-mini    │  │
│  │ █████████ GPT-5       │  │
│  │ ████████  Claude 4.7  │  │
│  │ ...                   │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 详细排名 (表格)        │  │  ← 表格区
│  │ 模型  |综合| 编码| 数学 │  │
│  │ o3—   62.9 55.8 87.2  │  │
│  │ ...                   │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  数据来源:                   │  ← 底部
│  artificialanalysis.ai      │
│  AI Daily © 2026            │
│  历史日报 →                  │
└─────────────────────────────┘
```

### 1.2 页面分区

| 区域 | 位置 | 内容 |
|------|------|------|
| 顶部导航栏 | 页面顶部，固定 | AI Daily 标题 + 日期 + 历史按钮 |
| 要点总结区 | 导航栏下方 | 50 字一句话总结 |
| 分类筛选栏 | 要点总结下方，sticky 吸顶 | 全部/新模型/大厂战略/工具推荐 |
| 条目列表区 | 筛选栏下方 | 10-15 条精选资讯 |
| 趋势看板区 | 条目列表下方 | 性能排行/价格/速度 三 Tab |
| 底部信息区 | 页面底部 | 数据来源 + 版权 + 历史入口 |

---

## 2. 交互流程

### 2.1 主要用户路径

```
路径 A：每日阅读流程（实际）
  打开页面 → BP 血压仪式 → 浏览 Hero（幸运模型+冷笑话）
  → 读 AI 脉搏摘要 → 趋势快照 → 趋势看板交互
  → 资讯列表展开/筛选 → 跳转原文

路径 B：分类筛选流程（实际）
  日报页面 → 点击筛选标签 [全部/新模型/大厂战略/工具推荐]
  → 列表实时过滤 → 点击条目展开

路径 C：历史回溯流程（实际）
  日报页面 → 顶部导航 "历史" → 弹出日期列表（30 天）
  → 点击日期 → fetch JSON → 内联替换当前页面内容
  → 趋势看板滚动到视野

路径 D：手动刷新流程（实际，仅本地 dev server）
  http://localhost:8080 → 点 ↻ 按钮 → API call → 轮询状态 → 刷新数据
  → 点击 "手动重试" 按钮 → 重新触发抓取 → 显示结果
```

### 2.2 用户操作与系统响应

| 用户操作 | 系统响应 |
|---------|---------|
| 点击微信推送消息 | 微信内打开日报页面 |
| 点击分类标签 | 列表过滤，标签高亮，URL hash 更新 |
| 点击条目 | 平滑展开 AI 概要区（200-300ms 动画） |
| 再次点击已展开条目 | 平滑收起概要区 |
| 点击另一条目 | 已展开条目自动收起，新条目展开 |
| 点击"查看原文" | 新窗口打开原始链接 |
| 点击趋势看板 Tab | 切换对应图表区 |
| 点击"历史日报" | 跳转归档页 |
| 点击手动重试按钮 | 触发全流程重跑，展示 loading |

### 2.3 AI 节点交互

```
节点 1：AI 摘要展示（条目展开时）
  触发 → 用户点击条目
  AI 行为 → 展示已生成的中文概要
  状态 → [默认态] 收起 → [展开态] 200-300 字概要加载（文本已在页面，无需请求）
  异常 → 生成失败时展示 "原文直达"

节点 2：AI 要点总结（页面加载时）
  触发 → 页面加载
  AI 行为 → 顶部展示 50 字一句话总结
  状态 → 文本直接渲染（已预生成）
  异常 → 无总结时不展示该区域

节点 3：分类标签切换（页面加载时）
  触发 → 条目列表渲染
  AI 行为 → 每条展示 AI 分配的分类标签
  状态 → 标签颜色区分（新模型蓝/大厂绿/工具橙）
```

---

## 3. AI 交互细节

| 交互点 | 触发方式 | AI 响应样式 | 加载处理 | 兜底展示 | 反馈机制 |
|--------|---------|------------|---------|---------|---------|
| 今日要点 | 页面加载自动展示 | 50 字内一句话，顶部黑色加粗文字 | 无加载（预生成） | 隐藏该区域 | 无直接反馈 |
| 条目概要 | 点击条目展开 | 200-300 字中文概要，左侧竖线引用样式，底部 [查看原文] 按钮 | 无加载（预生成） | "原文直达" + 标题 + 来源链接 | 原文链接可跳转 |
| 分类标签 | 条目渲染自动展示 | 彩色小标签：蓝=新模型，绿=大厂战略，橙=工具推荐 | 无加载（预生成） | 无标签（置信度不足时） | 点击标签可筛选 |
| 多来源标记 | 条目底部自动展示 | 灰色小字 "多来源报道: N 个来源" | 无加载 | 单来源不展示 | 点击展开来源列表 |
| 趋势图表 | 切换 Tab 查看 | Chart.js 渲染柱状图/表格 | 图表渲染中骨架屏 | 展示上次缓存 + 更新日期标注 | 数据来源可点击跳转 |

### 3.1 AI 输出区域与 UI 框架的区分

- AI 生成内容（概要、要点总结、分类标签）用 **浅灰底 + 左侧竖线** 标记
- 原始内容（标题、来源、时间）无特殊标记
- 趋势看板数据用 **顶部标注** "数据来源：artificialanalysis.ai" 区分

---

## 4. 原型标注

### 4.1 日报页面完整状态标注

```
【页面】AI Daily 日报阅读页

状态流：
  正常加载 → [要点总结区] 展示 → [筛选栏] 默认"全部"
          → [条目列表] 10-15 条，按重要性排序
          → [趋势看板] 默认展示"性能"Tab
          → [底部] 数据来源

  异常 A（信息源全失败）→ [要点总结区] 隐藏
                     → [条目列表区] 展示兜底："今日信息源暂不可用"
                     → [手动重试按钮] 可点击
                     → [趋势看板] 展示缓存数据

  异常 B（AI 摘要全失败）→ [条目列表] 每条仅展示标题 + 来源链接 + "原文直达"

  空态（首次访问）→ [要点总结区] 隐藏
                 → [条目列表区] 展示产品简介 + "明日将推送首期日报"
                 → [趋势看板] 正常展示

  加载态 → [要点总结区] 骨架屏闪烁
         → [条目列表] 骨架屏（仿条目形状占位）
         → [趋势看板] 骨架屏
```

### 4.2 条目展开状态

```
【条目】单条资讯展开

状态流：
  收起态（默认）→ 展示：类型标签 + 标题 + 来源 + 时间
  展开态（点击）→ 新增展示：AI 概要区（200-300 字）+ 查看原文按钮
  多来源（仅合并条目）→ 额外展示：来源列表
  执行失败 → "原文直达"替代概要区

标注信息：
  ✓ 概要区左侧有竖线标记（标识 AI 生成）
  ✓ 来源链接在概要区底部，可点击
  ✓ 展开/收起动画 200-300ms
  ✓ 同时只展开一条
```

### 4.3 趋势看板 Tab 切换

```
【趋势看板】Tab 切换

数据状态：
  正常 → API 最新数据渲染
  API 异常 → 缓存数据 + 顶部黄色提示 "数据更新于 YYYY-MM-DD"
  API 30 天未更新 → 红色提示 "数据超过 30 天未更新，请检查数据源"

各 Tab 内容：
  性能 Tab → 柱状图（Top 5）+ 性能数据表格（Top 10）
  价格 Tab → 模型价格对比表（模型名 / 输入 $/输出 $/混合 $）
  速度 Tab → 输出速度柱状图 + 首 Token 延迟对比图

标注信息：
  ✓ artificialanalysis.ai 链接在图表下方，可点击
  ✓ 图表使用 Chart.js，移动端响应式
  ✓ 三 Tab 点击无路由变化，纯前端切换
```

### 4.4 实际适配

- **桌面优先**：最大宽度 880px 居中，暗色赛博朋克风
- **亮/暗主题**：CSS 自定义属性切换，localStorage 持久化
- **趋势图自适应**：Chart.js responsive + maintainAspectRatio: false
- **移动端**：grid 自适应，小屏单列，筛选栏横向滚动
- **字体**：系统等宽/无衬线混排，'JetBrains Mono' + system-ui

---

## 5. 实际数据说明

### 5.1 数据来源

| 数据 | 来源 | 方式 |
|------|------|------|
| 日报资讯 | 15 RSS 源 | pipeline/run.py 每日抓取 + DeepSeek AI 处理 |
| 趋势看板 | artificialanalysis.ai API v2 | CF Worker 代理，降级到内联快照 |
| 历史日报 | data/daily/YYYY-MM-DD.json | 30 天历史（含补生成的模拟数据） |

### 5.2 模拟/降级数据

- **MOCK_NEWS**：10 条静态英文 mock，pipeline 数据加载后替换
- **MOCK_TREND**：LLM/图片/视频全模态 mock 数据（与 AA API 对齐）
- **REAL_TREND_DATA**：AA API 最新快照内联（部署版本使用）
| 大厂战略 | Google DeepMind 独立运营，母公司追加 10 亿美元投资 | The Verge | Alphabet 宣布 DeepMind 成为独立子公司... |
| 工具推荐 | Cursor 2.0 发布，支持多 Agent 协作编程 | 量子位 | Cursor 2.0 引入 Agent 协作模式，多个 Agent 可并行处理不同文件... |

### 5.2 模拟趋势数据

从 artificialanalysis.ai API 文档中的实际示例数据模拟展示。

